#!/bin/bash

# Rubyのインストール
sudo apt update
sudo apt install -y ruby-full

# バージョンの確認
ruby --version

#!/bin/bash

sudo apt install -y libsqlite3-dev nodejs yarn

# Railsのインストール
sudo gem install rails

# バージョンの確認
rails --version

